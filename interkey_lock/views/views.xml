<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="interkey_lock.list">
      <field name="name">interkey_lock list</field>
      <field name="model">interkey_lock.interkey_lock</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="interkey_lock.action_window">
      <field name="name">interkey_lock window</field>
      <field name="res_model">interkey_lock.interkey_lock</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="interkey_lock.action_server">
      <field name="name">interkey_lock server</field>
      <field name="model_id" ref="model_interkey_lock_interkey_lock"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="interkey_lock" id="interkey_lock.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="interkey_lock.menu_1" parent="interkey_lock.menu_root"/>
    <menuitem name="Menu 2" id="interkey_lock.menu_2" parent="interkey_lock.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="interkey_lock.menu_1_list" parent="interkey_lock.menu_1"
              action="interkey_lock.action_window"/>
    <menuitem name="Server to list" id="interkey_lock" parent="interkey_lock.menu_2"
              action="interkey_lock.action_server"/>
    -->
  </data>
</odoo>